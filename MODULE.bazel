module(
    name = "remote-apis",
    version = "0.0.0",
    compatibility_level = 0,
)

bazel_dep(name = "rules_proto", version = "5.3.0-21.7")
bazel_dep(name = "protobuf", version = "21.7", repo_name="com_google_protobuf")
# TODO: Make googleapis importable via bzlmod.
